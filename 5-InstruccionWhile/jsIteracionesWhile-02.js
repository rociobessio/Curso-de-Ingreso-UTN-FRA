/*
al presionar el botón mostrar 10 repeticiones 
con números DESCENDENTES, desde el 10 al 1.*/
function mostrar()
{
	let contador;

	contador=10;//Inicializa en 0 porque es de forma DESCENDENTE

	while(contador>0)//Mientras que contador(10) sea MENOR que 0 continuara
	{
		alert(contador);//Primero imprime el 10
		contador=contador-1;//Despues sigue restando.
							//Lo contrario de la forma ascendente.
	}
	







}//FIN DE LA FUNCIÓN
/*	contador=10; //Inicializo la condicion

	while(contador>0)//Hago la condicion
	{
		alert(contador); //PRIMERO MUESTRO Y LUEGO:
		contador=contador-1; //DECRECE
	} */